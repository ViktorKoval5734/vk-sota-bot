# 📋 Инструкция по переносу бота на GitHub

## 🎯 Что нужно перенести

### ✅ **ОБЯЗАТЕЛЬНО перенести (основные файлы):**
```
├── bot.py                 # Основной файл бота
├── config.py              # Конфигурация
├── gigachat_client.py     # Клиент Гигачата
├── history.py             # Работа с историей
├── user_preferences.py    # Персонализация
├── confirmation_manager.py # Менеджер кода подтверждения
├── message_deduplicator.py # Дедупликатор сообщений
├── requirements.txt       # Зависимости Python
├── README.md             # Документация
├── .env.example          # Шаблон переменных
├── .gitignore            # Исключения для Git
├── Dockerfile            # Для Docker
├── docker-compose.yml    # Для Docker
├── nginx.conf            # Конфигурация Nginx
├── render.yaml           # Для деплоя на Render
├── deploy.sh             # Скрипт деплоя
├── start_bot.sh          # Запуск бота
├── start_tunnel.sh       # Запуск туннеля
├── install_ngrok.sh      # Установка ngrok
├── simple_tunnel.py      # Простой туннель
├── update_confirmation.py # Обновление кода
└── SETUP_GUIDE.md        # Руководство по настройке
```

### ❌ **НЕ переносить (исключить):**
```
├── .env                  # Секретные токены - НИКОГДА!
├── history.json          # Локальная история
├── user_preferences.json # Локальные настройки
├── sota.env              # Локальная конфигурация
├── __pycache__/          # Python кэш
├── venv/                 # Виртуальное окружение
├── *.tgz                 # Архивы ngrok
├── wget-log              # Логи загрузки
└── DEPLOYMENT_GUIDE.md   # Устаревшие инструкции
    ├── QUICK_DEPLOY.md
    └── RENDER_DEPLOY_GUIDE.md
```

## 🚀 Пошаговая инструкция

### Шаг 1: Подготовка локального репозитория

```bash
cd /home/deck/vk_sota_bot

# Инициализация Git
git init

# Добавление всех файлов
git add .

# Коммит
git commit -m "Initial commit: Sota Sil VK Bot"
```

### Шаг 2: Создание репозитория на GitHub

1. **Зайдите на [github.com](https://github.com)**
2. **Нажмите "New repository"**
3. **Заполните:**
   - **Repository name:** `vk-sota-bot`
   - **Description:** `Sota Sil - VK Bot with GigaChat integration`
   - **Public** или **Private** (рекомендую Public для бесплатного Render)
   - ✅ **Add a README file** (НЕ ставить галочку, файл уже есть)
   - ✅ **Add .gitignore** (НЕ ставить галочку, файл уже есть)

### Шаг 3: Привязка к удалённому репозиторию

```bash
# Добавление удалённого репозитория
git remote add origin https://github.com/ВАШ_USERNAME/vk-sota-bot.git

# Отправка на GitHub
git branch -M main
git push -u origin main
```

### Шаг 4: Проверка файлов на GitHub

После загрузки убедитесь, что на GitHub есть:

✅ **Основные файлы бота**
✅ **.env.example** (но НЕ .env)
✅ **.gitignore**
✅ **README.md**
✅ **requirements.txt**

❌ **НЕТ секретных файлов:**
- .env
- history.json
- user_preferences.json
- venv/
- __pycache__/

## 🔧 Настройка Render.com

### 1. Подключение репозитория

1. **Зайдите на [render.com](https://render.com)**
2. **Подключите GitHub аккаунт**
3. **Выберите репозиторий `vk-sota-bot`**

### 2. Настройка переменных окружения

В панели Render добавьте **Environment Variables**:

```
VK_TOKEN=ваш_токен_ВК
VK_GROUP_ID=ваш_ID_группы
GIGACHAT_AUTH_KEY=ваш_ключ_Гигачат
GIGACHAT_CLIENT_ID=ваш_CLIENT_ID
CONFIRMATION_SECRET=ваш_секретный_ключ
VK_CONFIRMATION_CODE=код_подтверждения
```

### 3. Настройка деплоя

```
Name: sota-sil-bot
Runtime: Python 3
Build Command: pip install -r requirements.txt
Start Command: python bot.py
```

## 🧪 Проверка работы

### 1. Получите URL от Render
После деплоя получите URL: `https://sota-sil-bot.onrender.com`

### 2. Настройте ВКонтакте
- **Адрес сервера:** `https://sota-sil-bot.onrender.com`
- **Секретный ключ:** ваш `CONFIRMATION_SECRET`
- **Версия API:** `5.199`

### 3. Проверьте работу
Отправьте сообщение в беседу с упоминанием бота.

## ⚠️ Важные моменты

### Безопасность:
- **НИКОГДА не коммитьте .env файл**
- **Используйте только .env.example для шаблона**
- **Токены добавляйте только в панели Render**

### Обновление кода:
```bash
# После изменений локально
git add .
git commit -m "Описание изменений"
git push origin main
# Render автоматически обновит бота
```

## 📝 Список команд для копирования

```bash
# В директории бота
cd /home/deck/vk_sota_bot
git init
git add .
git commit -m "Initial commit: Sota Sil VK Bot"
git branch -M main
git remote add origin https://github.com/USERNAME/vk-sota-bot.git
git push -u origin main
```

## ✅ Готово!

После этих действий ваш бот будет:
- ✅ Развёрнут на Render.com
- ✅ Работать 24/7
- ✅ Иметь HTTPS URL
- ✅ Автоматически обновляться при коммитах

**Безопасность гарантирована** - все секреты остаются только в панели Render! 🔒